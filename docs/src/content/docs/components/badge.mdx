---
title: UIBadge
description: Label badge with semantic color tones for status and metadata.
---

import { ComponentPreview } from "../../../components/ComponentPreview.tsx";
import { UIBadge, UIColorToken, UIStatusTone } from "tango-api";

## Preview

<ComponentPreview client:load label="Tones">
  <div style={{ display: "flex", gap: "8px", flexWrap: "wrap" }}>
    <UIBadge label="Neutral" tone="neutral" />
    <UIBadge label="Info" tone="info" />
    <UIBadge label="Success" tone="success" />
    <UIBadge label="Warning" tone="warning" />
    <UIBadge label="Danger" tone="danger" />
  </div>
</ComponentPreview>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `label` | `string` | required | Badge text |
| `tone` | `"neutral" \| "info" \| "success" \| "warning" \| "danger"` | `"neutral"` | Semantic color |

## Aliases

`UIColorToken` and `UIStatusTone` are identical to `UIBadge` — use whichever reads best in your context.

## Usage

```tsx
import { UIBadge } from "tango-api";

<UIBadge label="Open" tone="success" />
<UIBadge label="Deprecated" tone="warning" />
<UIBadge label="Failed" tone="danger" />
```
