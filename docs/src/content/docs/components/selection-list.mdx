---
title: UISelectionList
description: Single or multi-select item list for picking from a set of options.
---

import { SelectionListSingleDemo, SelectionListMultiDemo } from "../../../components/demos/SelectionListDemo.tsx";

## Preview

<SelectionListSingleDemo client:load />

<SelectionListMultiDemo client:load />

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `items` | `Array<{ value: string; title: string; subtitle?: string }>` | required | Available items |
| `selected` | `string[]` | required | Currently selected values |
| `multiple` | `boolean` | `false` | Allow multiple selections |
| `onChange` | `(next: string[]) => void` | — | Called with updated selection array |

## Usage

```tsx
import { UISelectionList } from "tango-api";

const [selected, setSelected] = useState<string[]>([]);

<UISelectionList
  multiple
  items={languages}
  selected={selected}
  onChange={setSelected}
/>
```
