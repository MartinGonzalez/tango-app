---
title: UIIcon
description: SVG icon component with built-in icon set.
---

import { ComponentPreview } from "../../../components/ComponentPreview.tsx";
import { UIIcon, Icon } from "tango-api";

## Preview

<ComponentPreview client:load label="All icons">
  <div style={{ display: "flex", gap: "16px", flexWrap: "wrap", alignItems: "center" }}>
    <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "4px" }}>
      <UIIcon name={Icon.Branch} size={20} />
      <span style={{ fontSize: "11px", opacity: 0.6 }}>branch</span>
    </div>
    <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "4px" }}>
      <UIIcon name={Icon.Play} size={20} />
      <span style={{ fontSize: "11px", opacity: 0.6 }}>play</span>
    </div>
    <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "4px" }}>
      <UIIcon name={Icon.Post} size={20} />
      <span style={{ fontSize: "11px", opacity: 0.6 }}>post</span>
    </div>
    <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "4px" }}>
      <UIIcon name={Icon.AI} size={20} />
      <span style={{ fontSize: "11px", opacity: 0.6 }}>ai</span>
    </div>
    <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "4px" }}>
      <UIIcon name={Icon.Check} size={20} />
      <span style={{ fontSize: "11px", opacity: 0.6 }}>check</span>
    </div>
    <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "4px" }}>
      <UIIcon name={Icon.Pause} size={20} />
      <span style={{ fontSize: "11px", opacity: 0.6 }}>pause</span>
    </div>
    <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "4px" }}>
      <UIIcon name={Icon.ExternalLink} size={20} />
      <span style={{ fontSize: "11px", opacity: 0.6 }}>external-link</span>
    </div>
    <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "4px" }}>
      <UIIcon name={Icon.Send} size={20} />
      <span style={{ fontSize: "11px", opacity: 0.6 }}>send</span>
    </div>
    <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "4px" }}>
      <UIIcon name={Icon.Pencil} size={20} />
      <span style={{ fontSize: "11px", opacity: 0.6 }}>pencil</span>
    </div>
  </div>
</ComponentPreview>

<ComponentPreview client:load label="Sizes">
  <div style={{ display: "flex", gap: "12px", alignItems: "center" }}>
    <UIIcon name={Icon.Play} size={12} />
    <UIIcon name={Icon.Play} size={16} />
    <UIIcon name={Icon.Play} size={20} />
    <UIIcon name={Icon.Play} size={24} />
  </div>
</ComponentPreview>

## Available Icons

| Name | Constant |
|------|----------|
| `branch` | `Icon.Branch` |
| `play` | `Icon.Play` |
| `post` | `Icon.Post` |
| `ai` | `Icon.AI` |
| `check` | `Icon.Check` |
| `pause` | `Icon.Pause` |
| `external-link` | `Icon.ExternalLink` |
| `send` | `Icon.Send` |
| `pencil` | `Icon.Pencil` |

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `name` | `UIIconName` | required | Icon identifier |
| `className` | `string` | — | Additional CSS class |
| `size` | `number` | `16` | Icon size in pixels |
| `title` | `string` | — | Accessible title (makes icon non-decorative) |

## Usage

```tsx
import { UIIcon, Icon } from "tango-api";

<UIIcon name={Icon.Check} size={20} title="Complete" />
```
