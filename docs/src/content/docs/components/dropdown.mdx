---
title: UIDropdown
description: Custom dropdown with React-managed state, placeholder support, and keyboard navigation.
---

import { ComponentPreview } from "../../../components/ComponentPreview.tsx";
import { UIDropdown } from "tango-api";

## Preview

<ComponentPreview client:load label="With placeholder">
  <UIDropdown
    placeholder="Choose a framework..."
    options={[
      { value: "react", label: "React" },
      { value: "vue", label: "Vue" },
      { value: "svelte", label: "Svelte" },
      { value: "angular", label: "Angular" },
    ]}
  />
</ComponentPreview>

<ComponentPreview client:load label="With initial value">
  <UIDropdown
    initialValue="vue"
    options={[
      { value: "react", label: "React" },
      { value: "vue", label: "Vue" },
      { value: "svelte", label: "Svelte" },
    ]}
  />
</ComponentPreview>

<ComponentPreview client:load label="Disabled">
  <UIDropdown
    placeholder="Can't touch this"
    disabled
    options={[
      { value: "a", label: "Option A" },
    ]}
  />
</ComponentPreview>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `options` | `Array<{ value: string; label: string }>` | required | Available options |
| `value` | `string` | — | Controlled value |
| `initialValue` | `string` | — | Initial value for uncontrolled mode |
| `placeholder` | `string` | `"Select option"` | Placeholder when nothing is selected |
| `disabled` | `boolean` | `false` | Disables the dropdown |
| `onChange` | `(value: string) => void` | — | Called when selection changes |

## Usage

```tsx
import { UIDropdown } from "tango-api";

// Uncontrolled
<UIDropdown
  placeholder="Pick one..."
  options={frameworks}
  onChange={(value) => console.log(value)}
/>

// Controlled
<UIDropdown
  value={selected}
  options={frameworks}
  onChange={setSelected}
/>
```
