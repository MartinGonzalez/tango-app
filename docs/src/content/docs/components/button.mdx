---
title: UIButton
description: Action button with label, optional icon, and multiple style variants.
---

import { ComponentPreview } from "../../../components/ComponentPreview.tsx";
import { UIButton, Icon } from "tango-api";

## Preview

<ComponentPreview client:load label="Variants">
  <div style={{ display: "flex", gap: "8px", flexWrap: "wrap" }}>
    <UIButton label="Primary" variant="primary" />
    <UIButton label="Secondary" variant="secondary" />
    <UIButton label="Ghost" variant="ghost" />
    <UIButton label="Danger" variant="danger" />
    <UIButton label="Success" variant="success" />
  </div>
</ComponentPreview>

<ComponentPreview client:load label="Sizes">
  <div style={{ display: "flex", gap: "8px", alignItems: "center" }}>
    <UIButton label="Small" variant="primary" size="sm" />
    <UIButton label="Medium" variant="primary" size="md" />
  </div>
</ComponentPreview>

<ComponentPreview client:load label="With icon">
  <div style={{ display: "flex", gap: "8px" }}>
    <UIButton label="Send" variant="primary" icon={Icon.Send} />
    <UIButton label="Edit" variant="secondary" icon={Icon.Pencil} />
  </div>
</ComponentPreview>

<ComponentPreview client:load label="Disabled & full width">
  <div style={{ display: "flex", flexDirection: "column", gap: "8px" }}>
    <UIButton label="Disabled" variant="primary" disabled />
    <UIButton label="Full Width" variant="primary" fullWidth />
  </div>
</ComponentPreview>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `label` | `string` | required | Button text |
| `icon` | `UIIconName \| ReactNode` | — | Icon to the left of the label |
| `variant` | `"primary" \| "secondary" \| "ghost" \| "danger" \| "success"` | `"secondary"` | Visual style |
| `size` | `"sm" \| "md"` | `"md"` | Button size |
| `disabled` | `boolean` | `false` | Disables the button |
| `fullWidth` | `boolean` | `false` | Stretches to fill container width |
| `onClick` | `() => void` | — | Click handler |

## Usage

```tsx
import { UIButton, Icon } from "tango-api";

<UIButton label="Save" variant="primary" icon={Icon.Check} onClick={handleSave} />
```
