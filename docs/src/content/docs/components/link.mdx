---
title: UILink
description: Styled hyperlink with external link detection.
---

import { ComponentPreview } from "../../../components/ComponentPreview.tsx";
import { UILink } from "tango-api";

## Preview

<ComponentPreview client:load label="Default">
  <div style={{ display: "flex", gap: "16px" }}>
    <UILink href="https://github.com" label="GitHub" />
    <UILink href="/docs" label="Internal link" />
  </div>
</ComponentPreview>

<ComponentPreview client:load label="Custom color">
  <UILink href="https://example.com" label="Custom colored link" color="#10b981" />
</ComponentPreview>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `href` | `string` | required | Link URL |
| `label` | `string` | required | Link text |
| `color` | `string` | — | Custom link color (sets `--tui-link-color`) |
| `external` | `boolean` | auto-detected | Forces external link behavior (`target="_blank"`) |
| `onClick` | `(event: React.MouseEvent<HTMLAnchorElement>) => void` | — | Click handler |

External links (starting with `http://` or `https://`) are auto-detected and open in a new tab.

## Usage

```tsx
import { UILink } from "tango-api";

<UILink href="https://docs.tango.dev" label="Documentation" />
```
